# 🚂 Railway 배포 가이드 (Private 24/7)

**목표:** PersonaBot을 프라이빗하게 24/7 실행

---

## ✅ **준비 완료!**

다음 파일들이 자동으로 생성되었습니다:
- ✅ `railway.json` - Railway 설정
- ✅ `Procfile` - 실행 명령
- ✅ `requirements.txt` - 의존성 (Gradio 추가됨)
- ✅ `app_gradio.py` - 클라우드 배포용 포트 설정 추가됨
- ✅ `.gitignore` - 민감 정보 제외

---

## 🚀 **Railway 배포 (10분 완성)**

### 1단계: Railway 계정 생성

1. https://railway.app 접속
2. **"Start a New Project"** 클릭
3. GitHub 계정으로 로그인

**무료 크레딧:**
```
💰 $5 크레딧/월 (매월 충전)
⏰ 소규모 앱은 무료로 충분!
```

---

### 2단계: GitHub Repository 생성

```bash
# 1. Git 초기화 (아직 안했다면)
git init

# 2. 파일 추가
git add .

# 3. 커밋
git commit -m "Initial commit for Railway deployment"

# 4. GitHub에 새 Repository 생성
# https://github.com/new
# Repository name: PersonaBot
# Private 선택 ✅

# 5. GitHub에 Push
git remote add origin https://github.com/YOUR_USERNAME/PersonaBot.git
git branch -M main
git push -u origin main
```

**⚠️ 중요:** `.env` 파일은 자동으로 제외됩니다 (`.gitignore`에 포함됨)

---

### 3단계: Railway에 배포

#### Railway에서:

1. **"New Project"** 클릭
2. **"Deploy from GitHub repo"** 선택
3. **Repository 선택:** `PersonaBot`
4. **자동 배포 시작** 🎉

---

### 4단계: 환경 변수 설정

Railway 프로젝트에서:

1. **"Variables"** 탭 클릭
2. 다음 환경 변수 추가:

```bash
OPENAI_API_KEY=sk-proj-your-api-key-here
```

**저장 후 자동 재배포됨!**

---

### 5단계: 도메인 확인

1. **"Settings"** 탭 클릭
2. **"Generate Domain"** 클릭

**결과:**
```
🌐 https://personabot-production.up.railway.app
🔐 Username: sgrfuture
🔐 Password: misanee
```

---

## 🎯 **완료!**

### 접속 정보:

```
URL: https://personabot-production.up.railway.app
Username: sgrfuture
Password: misanee

✅ 24/7 실행
✅ 완전 프라이빗 (코드 비공개)
✅ 자동 배포 (GitHub Push 시)
✅ 고정 URL
```

---

## 💰 **비용 예상**

Railway는 **사용량 기반 과금**:

| 항목 | 예상 비용 |
|------|----------|
| **무료 크레딧** | $5/월 |
| **소규모 트래픽** | ~$2-3/월 |
| **중규모 트래픽** | ~$5-10/월 |

**결론:** 무료 크레딧으로 충분할 가능성 높음! 🎉

---

## 🔧 **업데이트 방법**

코드 수정 후:

```bash
# 1. 변경사항 커밋
git add .
git commit -m "Update feature"

# 2. GitHub에 Push
git push

# 3. Railway 자동 배포! ✅
```

---

## 📊 **모니터링**

Railway Dashboard에서 확인 가능:

- ✅ CPU/메모리 사용량
- ✅ 로그 실시간 확인
- ✅ 비용 사용량
- ✅ 배포 히스토리

---

## 🆚 **다른 옵션과 비교**

| 특징 | Railway | Render | HF Spaces (Private) |
|------|---------|--------|---------------------|
| **무료 크레딧** | $5/월 | ❌ | ❌ |
| **슬립 모드** | ❌ | ✅ (15분) | ❌ |
| **자동 배포** | ✅ | ✅ | ✅ |
| **Private** | ✅ | ✅ | ✅ |
| **비용** | $0-5/월 | $7~/월 | $5~/월 |
| **추천도** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐⭐⭐ |

---

## ❓ **문제 해결**

### 문제 1: 배포 실패
```bash
# Railway 로그 확인
# "Deployments" 탭 → 최신 배포 클릭 → "View Logs"
```

### 문제 2: 환경 변수 누락
```bash
# Variables 탭에서 OPENAI_API_KEY 확인
```

### 문제 3: 포트 오류
```bash
# app_gradio.py가 PORT 환경변수를 자동으로 읽음 (이미 수정됨)
```

---

## 🎊 **성공!**

이제 다음을 얻었습니다:

✅ **24/7 실행** - 컴퓨터 끄고 자도 됨!  
✅ **프라이빗** - 코드 완전 비공개  
✅ **고정 URL** - 친구들과 공유 가능  
✅ **자동 배포** - Git Push만 하면 끝  
✅ **무료** - $5 크레딧으로 충분  

---

**다음 단계:** GitHub에 코드 Push 후 Railway에서 배포하세요!

```bash
git push origin main
```

**Railway가 자동으로:**
1. 코드 빌드 ✅
2. 의존성 설치 ✅
3. 앱 실행 ✅
4. URL 생성 ✅

**5-10분 후 완료!** 🎉

